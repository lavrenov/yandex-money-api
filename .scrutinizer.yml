build:
  nodes:
    analysis:
      tests:
        override:
          - php-scrutinizer-run

filter:
  excluded_paths:
    - "tests/"
  dependency_paths:
    - "vendor/"

tools:
  external_code_coverage:
    timeout: 600
    runs: 3 
